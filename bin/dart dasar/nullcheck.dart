void main(){
  //int? angka = null; *tidak bisa digunakan karena data null

  //print(angka.toDouble()); *tidak bisa digunakan karena data null

  int?age = null;

  if(age != null) {
    print(age.toDouble());
  }
}